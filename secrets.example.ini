; ============================================================
;  SECRETS PlatformIO — copier vers secrets.ini et renseigner.
;
;    cp secrets.example.ini secrets.ini
;
;  Ce fichier est suivi par git (aucune valeur réelle).
;  secrets.ini est ignoré par git (contient les vraies valeurs).
;
;  Le mot de passe ici DOIT correspondre à OTA_PASSWORD dans
;  include/secrets.h (les deux sont vérifiés lors de l'upload OTA).
; ============================================================

[secrets]
ota_password = change_me
